"""
Конфигурация проекта локальной вопросно-ответной системы по НПА ИБ.
"""

import os
from pathlib import Path

# Базовые пути
BASE_DIR = Path(__file__).parent
DATA_DIR = BASE_DIR / "data" / "laws"
CHROMA_DIR = BASE_DIR / "chroma_db"
OLLAMA_DIR = BASE_DIR / "ollama"
MODELS_CACHE = BASE_DIR / "models_cache"

# Настройки Ollama
OLLAMA_HOST = "127.0.0.1:11434"
OLLAMA_MODEL = "gemma4:e4b"          # Можно заменить на "llama3.1:8b" или другую
OLLAMA_TIMEOUT = 120

# Настройки эмбеддингов (для обычного RAG)
EMBEDDING_MODEL = "bge-m3"

# RAPTOR настройки 
RAPTOR_EMBEDDING_MODEL = "bge-m3"

# ========== НАСТРОЙКИ ЧАНКИНГА ==========
CHUNK_SIZE = 800                     # размер чанка в символах
CHUNK_OVERLAP = 150                  # перекрытие 
CHUNK_SEPARATORS = ["\n\n", "\n", ". ", "! ", "? ", "; ", ":\n", " ", ""]

# ========== НАСТРОЙКИ ПОИСКА ==========
RETRIEVAL_TOP_K = 15                 # сколько чанков изначально извлекается (может быть изменено в UI)
NEIGHBOUR_CHUNKS = 1                 # сколько соседних чанков добавлять с каждой стороны
SIMILARITY_THRESHOLD = 0.7           # порог релевантности (пока не используется)
USE_HYBRID_SEARCH = True
BM25_WEIGHT = 0.7                    # приоритет ключевым словам
VECTOR_WEIGHT = 0.3                  # семантическая близость
USE_MMR = False                      # Maximum Marginal Relevance (опционально)

# ========== НАСТРОЙКИ ГЕНЕРАЦИИ ==========
TEMPERATURE = 0.1                    # минимальная креативность для точных юридических ответов
TOP_P = 0.95
TOP_K = 64
MAX_TOKENS = 4096

SYSTEM_PROMPT = """Ты — эксперт по информационной безопасности и нормативно-правовым актам РФ.

Твоя задача — дать точный ответ на вопрос пользователя, основываясь **исключительно** на предоставленных фрагментах документов.

Правила работы:
1. Внимательно прочитай все фрагменты. Каждый фрагмент содержит текст из закона, приказа или инструкции.
2. Если в любом фрагменте есть информация, отвечающая на вопрос, обязательно используй её.
3. В ответе обязательно указывай источник: название документа и, если указано, номер статьи, часть, страницу или чанк.
4. Если в предоставленных фрагментах **нет** точного ответа на вопрос, скажи строго: 
   «В предоставленных нормативно-правовых актах информация по данному вопросу не найдена.»
5. **Запрещено** использовать собственные знания, додумывать факты, выдумывать номера статей или нормативы, отсутствующие в контексте.
6. Отвечай на русском языке, чётко и по существу.

Будь внимателен. От твоей точности зависит юридическая корректность."""
# =========================================

# Режим отладки (управляется переменной окружения RAG_DEBUG)
DEBUG = os.environ.get("RAG_DEBUG", "false").lower() in ("true", "1", "yes")

# Создаём необходимые папки
for dir_path in [DATA_DIR, CHROMA_DIR, OLLAMA_DIR, MODELS_CACHE]:
    dir_path.mkdir(parents=True, exist_ok=True)